#include "main.h"

/**
 * _isdigit - the function below tells if a value is a digit or not
 * @c: this is the function paramter that carries the string value or character
 * Return: 1 if execution is correct and a negative value when wri=ong execution occurs
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
