#include "main.h"

/**
 *_islower - is usd to find chareacters of lower case
 *	from numericals
 *@c: this is the function parameter for determinining
 *	character case
 *
 *Return: returns 1 on success
 */

int _islower(int c)
{
	int m = 'a';

	for (m = 'a'; m <= 'z'; m++)
	{
		if (m == 'a')
			return (1);
		return (1);
	}
	return (0);
}

