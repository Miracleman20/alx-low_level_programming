#include "main.h"
#include <stdlib.h>

/**
 *strtow - a function preprocessor for splitting a
 *	string into two
 *
 * @str: is the string to be split
 *
 *Return: returns a positive value
 */

char **strtow(char *str)
{
	int i;
	char *arr;

	/**
	 * arr = malloc(sizeof())
	 */

	if (str == NULL || str == "")
		return (NULL);

	if (arr == NULL)
		return (NULL);
}
